# Extra index so torch installs the small CPU-only build (~200MB) instead
# of the default multi-GB CUDA build. pip checks PyPI first, then this.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core app ---
streamlit
pypdf
python-docx

# --- AI model (local, free, no API key) ---
torch
transformers
sentencepiece
